DATABASE_URL="postgresql://user@localhost:5432/dynamify_dev?schema=public"
TEST_DATABASE_URL="postgresql://user@localhost:5432/dynamify_test?schema=public"
SESSION_COOKIE_NAME="dynamify_session"
# Ecommerce foundation only — generate with `openssl rand -hex 32`. Unset
# until a real platform connection needs to be stored.
# PLATFORM_CREDENTIALS_ENCRYPTION_KEY=""
